name: Blue-Green Deploy

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Docker image tag to deploy"
        required: true
      environment:
        description: "Target environment"
        required: true
        default: "prod"
        type: choice
        options: [prod, staging]
  push:
    branches: [main]
    paths:
      - "api/**"
      - "services/**"
      - "Dockerfile"
      - "api/Dockerfile"

concurrency:
  group: blue-green-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    name: Build & Push Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}
      image_digest: ${{ steps.push.outputs.digest }}
    steps:
      - uses: actions/checkout@v4

      - name: Compute image name
        id: image
        run: |
          # GHCR rejects uppercase path segments and this repository's name
          # starts with a dash, so build the path from a lowercased owner.
          NAMESPACE=$(echo "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')
          echo "name=${REGISTRY}/${NAMESPACE}/stellar-oracle-api" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=sha,prefix=sha-
            type=ref,event=branch
            type=raw,value=${{ github.event.inputs.image_tag }},enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        id: push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./api/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Blue-Green Deploy
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'prod' }}
    steps:
      - uses: actions/checkout@v4

      - name: Check deployment prerequisites
        id: guard
        env:
          KUBECONFIG_B64: ${{ secrets.KUBECONFIG }}
        run: |
          if [ -z "$KUBECONFIG_B64" ]; then
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "::notice::KUBECONFIG is not set — skipping the blue-green deploy."
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up kubectl
        if: steps.guard.outputs.configured == 'true'
        uses: azure/setup-kubectl@v3
        with:
          version: "v1.29.0"

      - name: Configure kubeconfig
        if: steps.guard.outputs.configured == 'true'
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Determine image tag
        id: image
        run: |
          TAG="${{ github.event.inputs.image_tag || needs.build-and-push.outputs.image_tag }}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Run blue-green deploy
        if: steps.guard.outputs.configured == 'true'
        env:
          NAMESPACE: stellar-oracle
          IMAGE_TAG: ${{ steps.image.outputs.tag }}
        run: |
          chmod +x scripts/deploy-blue-green.sh
          ./scripts/deploy-blue-green.sh "$IMAGE_TAG"

      - name: Verify deployment
        if: steps.guard.outputs.configured == 'true'
        run: |
          kubectl rollout status deployment -l app=stellar-oracle-api \
            -n stellar-oracle --timeout=120s

      - name: Post deployment summary
        if: always()
        run: |
          echo "## Deployment Summary" >> "$GITHUB_STEP_SUMMARY"
          echo "| Key | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|-----|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Image Tag | ${{ steps.image.outputs.tag }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Environment | ${{ github.event.inputs.environment || 'prod' }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Triggered by | ${{ github.actor }} |" >> "$GITHUB_STEP_SUMMARY"

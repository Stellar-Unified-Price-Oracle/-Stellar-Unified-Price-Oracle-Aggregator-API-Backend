apiVersion: v1
kind: Secret
metadata:
  name: sandbox-credentials
  namespace: stellar-oracle-sandbox
type: Opaque
stringData:
  # Replace before applying; never commit a real token.
  reset-token: replace-with-a-local-sandbox-reset-token

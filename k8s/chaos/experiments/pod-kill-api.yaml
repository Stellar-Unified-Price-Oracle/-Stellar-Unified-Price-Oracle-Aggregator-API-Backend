apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-api
  labels:
    chaos.stellar-oracle.io/experiment: pod-kill-api
    chaos.stellar-oracle.io/target: api
spec:
  action: pod-kill
  mode: one
  duration: 2m
  selector:
    namespaces:
      - stellar-oracle
    labelSelectors:
      app: api
